"""
lib — Shared Python utilities for the SAFE AI Production project.

Modules:
    file_info   File metadata extraction (FileInfo dataclass + loader)
    rules       Declarative rule engine (Rule, RuleGroup, evaluator registry)
    config      YAML/JSON config loader with auto-discovery
"""
