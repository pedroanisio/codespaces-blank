I want to design and implement a complete end-to-end pipeline for video production that can take a single high-level creative idea and fully develop it into a finished, consumable video.The pipeline must autonomously handle every stage in the correct sequential order, including all iterative refinement cycles at each stage. Specifically, it must cover:Script and narrative development
Character creation and development
Directorial vision and style definition
Visual direction, cinematography, and art style
Sound design, music, and audio effects (SFX)
All other production elements required for a complete video

At the end of the pipeline, the system must output a fully populated, structured schema (e.g., JSON, database record, or equivalent) containing every asset, metadata, and deliverable so that the final video can be directly rendered or consumed by a downstream production/rendering system.Question:
How many distinct skills (or modular AI agents / components) would I need to create to enable this full autonomous pipeline?

