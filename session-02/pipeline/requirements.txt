# ── Video assembly ─────────────────────────────────────────────────────────────
moviepy>=2.1.2
numpy>=1.24
Pillow>=10.0
requests>=2.31
jsonschema>=4.21

# ── Core AI providers ──────────────────────────────────────────────────────────
anthropic>=0.45.0          # Claude Sonnet 4.6 — primary skill executor (S01-S24)
openai>=1.60.0             # GPT-4.1 · GPT-Image-1.5 · TTS-HD (S12/S13)
google-genai>=1.5.0        # Gemini 2.5 Flash + Imagen 4 (image fallback)
google-generativeai>=0.8.6 # Google Generative AI legacy SDK
python-dotenv>=1.0.0       # Load .env API keys automatically

# ── Utilities ──────────────────────────────────────────────────────────────────
tqdm>=4.66                 # Progress bars for multi-skill pipeline runs

# ── Optional: audio generation (audiocraft / MusicGen) ────────────────────────
# Requires Python ≤3.11 — install manually if needed:
#   pip install torch==2.1.0 torchaudio==2.1.0 audiocraft==1.3.0
